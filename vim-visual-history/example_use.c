
    a = mean(data)                               // 1st selection (char)


    b = more_data                                // 2nd selection (line)


    if answer == 42 {                            //
        if bugs == 'bunny' {                     //
            if injury == 'but a scratch' {       // 3th selection (block)
                // do the thing                  //
            }                                    //
        }                                        //
    }                                            //


    if answer == 42 {                            // 6th selection
        if bugs == 'bunny' {                     // 5th selection
            if injury == 'but a scratch' {       // 4th selection (text object)
                // do the thing                  // 4th selection
            }                                    // 5th selection
        }                                        // 6th selection
    }
