
    a = mean(data)


    b = more_data


    c = len(fancy)


    d = st.bootstrap(other)


    e = np.mean(st.zscore(myarry), axis=1, dtype=np.float64)


    f = st.bootstrap(np.mean(data),
                        argA, argB)


    g = np.mean(st.zscore(myarry),
                axis=1, dtype=np.float64)


    [1, 2, 3, 4]
